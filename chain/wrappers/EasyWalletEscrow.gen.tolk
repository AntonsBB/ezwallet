// Auto-generated wrapper for contract 'EasyWalletEscrow'
//
// This file is automatically generated by 'acton wrapper'
// Do not edit manually — changes will be overwritten

import "@acton/build"
import "@acton/emulation/network"
import "@acton/testing/assert"
import "@contracts/types"

/// Storage `EscrowStorage` is defined in `@contracts/types`
struct EasyWalletEscrow {
    address: address
    stateInit: ContractState? = null
}

/// Creates a contract wrapper instance from the storage data
fun EasyWalletEscrow.fromStorage(
    storage: EscrowStorage,
    toShard: AddressShardingOptions? = null,
): EasyWalletEscrow {
    val stateInit = ContractState {
        code: build("EasyWalletEscrow"),
        data: storage.toCell(),
    };
    val address = AutoDeployAddress { stateInit, toShard }.calculateAddress();
    return EasyWalletEscrow { address, stateInit };
}

/// Creates a contract wrapper instance from the address
fun EasyWalletEscrow.fromAddress(address: address): EasyWalletEscrow {
    return EasyWalletEscrow { address };
}

/// Deploys the contract to the blockchain
fun EasyWalletEscrow.deploy(self, from: address, config: SendParams = {}): SendResultList {
    if (self.stateInit == null) {
        Assert.fail("Cannot deploy a contract created with 'fromAddress' because it lacks state init for deployment");
    }
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: { stateInit: self.stateInit },
    });
    return net.send(from, genericMsg);
}

fun EasyWalletEscrow.sendFundEscrow(
    self,
    from: address,
    queryId: uint64,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: FundEscrow { queryId },
    });
    return net.send(from, genericMsg);
}

fun EasyWalletEscrow.sendMarkDelivered(
    self,
    from: address,
    queryId: uint64,
    evidenceHash: uint256,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: MarkDelivered { queryId, evidenceHash },
    });
    return net.send(from, genericMsg);
}

fun EasyWalletEscrow.sendConfirmDelivery(
    self,
    from: address,
    queryId: uint64,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: ConfirmDelivery { queryId },
    });
    return net.send(from, genericMsg);
}

fun EasyWalletEscrow.sendOpenDispute(
    self,
    from: address,
    queryId: uint64,
    reasonHash: uint256,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: OpenDispute { queryId, reasonHash },
    });
    return net.send(from, genericMsg);
}

fun EasyWalletEscrow.sendRefundExpired(
    self,
    from: address,
    queryId: uint64,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: RefundExpired { queryId },
    });
    return net.send(from, genericMsg);
}

fun EasyWalletEscrow.sendReleaseAfterReview(
    self,
    from: address,
    queryId: uint64,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: ReleaseAfterReview { queryId },
    });
    return net.send(from, genericMsg);
}

fun EasyWalletEscrow.sendResolveDispute(
    self,
    from: address,
    queryId: uint64,
    releaseToSeller: bool,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: ResolveDispute { queryId, releaseToSeller },
    });
    return net.send(from, genericMsg);
}

/// Send message to the contract with a custom body cell
fun EasyWalletEscrow.sendAny(
    self,
    from: address,
    body: cell,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body,
    });
    return net.send(from, genericMsg);
}

fun EasyWalletEscrow.escrowData(self): EscrowStorage {
    return net.runGetMethod(self.address, "escrowData");
}

fun EasyWalletEscrow.expectedFundingValue(self): coins {
    return net.runGetMethod(self.address, "expectedFundingValue");
}
